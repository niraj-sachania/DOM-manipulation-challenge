// Select DOM elements
const input = 
const addBtn = 
const list = 

// Add event listener for button click

  if (text !== '') {
    // 1️. Create new <li>

    // 2️. Append to <ul>

    // 3️. Clear input

  } else {
    alert('Please enter a valid item!');
  };
